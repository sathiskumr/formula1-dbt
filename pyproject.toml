[project]
name = "formula1-dbt"
version = "0.1.0"
description = "transform layers for formula1 project in dbt"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dbt-core>=1.11.12",
    "dbt-databricks>=1.10.9",
]
